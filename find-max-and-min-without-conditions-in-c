#include <stdio.h>
#include <stdlib.h> 
int main() {
    int a = 10, b = 20;
    int maximum = (a + b + abs(a - b)) / 2;
    int minimum = (a + b - abs(a - b)) / 2;
    printf("Max: %d", maximum);
    printf("Min: %d", minimum);
    return 0;
}

#include<stdio.h>
int main()
{
 int a,b;
 printf("enter any two numbers:\n");
 scanf("%d %d",&a,&b);
 while(b!=0){
   int carry = a&b;
     a=a^b;
     b=carry<<1;
     }
   printf("sum:%d",a);
   return 0;
}
